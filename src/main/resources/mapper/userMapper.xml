<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.bit.management.manual_management.dao.userDao" ><!--命名空间，指定对应的Dao-->
    <!--<sql id="BASE_TABLE">-->
    <!--t_user-->
  <!--</sql>-->

    <!--<sql id="BASE_COLUMN">-->
    <!--userId,userName,password,phone-->
  <!--</sql>-->

    <insert id="insert" parameterType="com.bit.management.manual_management.entity.user">
        INSERT INTO
        user(
        userName,
        userPwd,
        roleId
        )VALUES (
        #{userName},
        #{userPwd},
        #{roleId}
        )
    </insert>

    <select id="selectUsers" resultType="com.bit.management.manual_management.entity.user" parameterType="com.bit.management.manual_management.entity.user">
        SELECT * FROM users WHERE name = #{userName}
    </select>


</mapper>